name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build_test_deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install

      - run: echo "Building..." # replace with your build cmd

      - run: npm test

      - if: github.ref == 'refs/heads/main'
        run: |
          echo "Deploying..."
          # npm run deploy
